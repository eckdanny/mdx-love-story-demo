import Child from './child'
import StatefulButton from './StatefulButton'

## Composing

I am the `parent` MDX document.

<Child />

## Using React Components

Import the `Button` component and use it in your markdown document:

<Button>Button</Button>

<p className="mt-3">Use props too:</p>

<Button color="info">Info</Button>{' '}
<Button color="success">Success</Button>{' '}
<Button color="dark">Dark</Button>

<p className="mt-3">Cool!</p>

## Stateful Components in Markdown?

You bet!

<StatefulButton>I HAVE STATE</StatefulButton>

<p className="mt-3">Not bad for Markdown, rt?</p>
